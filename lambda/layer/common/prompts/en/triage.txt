You are a security analyst. Determine if the following text contains actionable Threat Intelligence with specific Indicators of Compromise (IoCs) that can be used for detection and blocking.

[IMPORTANT] Consider articles that match the following as NOT containing threat intelligence (is_threat: false), even if they contain specific IoCs (hash values, IP addresses, domains, etc.):
- Articles that do not contain ANY specific IoCs such as Hash values (MD5, SHA-256, etc.), specific malicious IP addresses, or malicious domains/URLs. (MOST IMPORTANT)
- Articles about supply chain attacks involving npm, PyPI, or other package managers (excluded even if specific IoCs are present)
- Articles about malware targeting macOS, iOS, or Android (excluded even if specific IoCs are present)
- Articles that only mention malware names (e.g., XenoRAT, LockBit) or abstract descriptions of attack methods (e.g., phishing, abusing LNK files, using GitHub as C2) but lack the concrete IoCs needed to identify them.
- Short news summaries, periodic digests (e.g., Daily/Hourly Summary), or articles consisting only of links to external original articles (e.g., "Read more ->").
- Articles that only mention ransomware damage/victims.
- Articles that only report the occurrence of data breaches or unauthorized access at specific companies or organizations.
- Articles that only mention the discovery or disclosure of vulnerabilities, or related software updates.
- Articles that only mention tech support scam victims.
- Articles that only mention Denial of Service (DoS) or Distributed Denial of Service (DDoS) attacks.
- Articles that only report on law enforcement operations, arrests, or the takedown of servers and domains, without providing specific technical IoCs.

Definitions of specific threat information (IoCs):
- Valid IoCs: Specific malicious IP addresses, domains, URLs, hash values, specific file paths or registry keys, specific command lines (e.g., malicious PowerShell commands), or custom detection rules (e.g., YARA/Sigma).
  - [NOTE] Information stating only "how many IPs were found" is NOT an IoC.
  - [NOTE] CVE numbers are NOT IoCs.
  - [NOTE] If there is no specific value listed (e.g., "192.168.1.1" or "example.com"), it is NOT an IoC.

Return ONLY the following JSON object: {"is_threat": boolean, "confidence": "high"|"medium"|"low", "relevant_image_urls": ["url1", "url2"], "reason": "short explanation", "summary": "1-2 sentence summary of the article in English"}
  - "relevant_image_urls": A list of URLs from the "Image URLs found in article" section that appear to contain diagrams, screenshots of code, or attack flows. Exclude generic icons or banners.
  - "is_threat": Set to true ONLY if specific IoCs are present, otherwise false.

Text:
{{TEXT}}
